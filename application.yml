# 热部署相关
spring:
  devtools:
    restart:
      enabled: true

domain: default

# 配置eureka客户端
eureka:
  instance:
    hostname: localhost
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:9000/eureka/
